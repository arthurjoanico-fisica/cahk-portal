(()=>{const URL='https://ekmzeqnnktdwvzxacbix.supabase.co',KEY='sb_publishable_jeWoJ4G9UXN6ucS3WkZVzA_ytTEzuJz';const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const localDate=()=>{const d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`};async function load(){try{const r=await fetch(`${URL}/functions/v1/portal-public`,{method:'POST',headers:{'Content-Type':'application/json','apikey':KEY},body:JSON.stringify({action:'campus',date:localDate()})});const d=await r.json();if(!r.ok||d.error)throw new Error(d.error||'Erro');renderRu(d.ru);renderBus(d.intercampi)}catch(e){$('#ruMenu').innerHTML='<div class="campus-empty">Não foi possível buscar o cardápio agora. Use a fonte oficial.</div>';$('#nextBuses').innerHTML='<span class="next-bus">Consulte o horário oficial</span>'}}function renderRu(ru){$('#ruOfficial').href=ru.source;$('#ruDate').textContent=new Date(`${ru.date}T12:00:00`).toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'});if(!ru.menu||(!ru.menu.cafe&&!ru.menu.almoco&&!ru.menu.jantar)){$('#ruMenu').innerHTML='<div class="campus-empty">O cardápio de hoje não pôde ser lido automaticamente. Clique em “Fonte oficial”.</div>';return}const meals=[['Café da manhã',ru.menu.cafe],['Almoço',ru.menu.almoco],['Jantar',ru.menu.jantar]].filter(x=>x[1]);$('#ruMenu').innerHTML=`<div class="meal-grid">${meals.map(([a,b])=>`<div class="meal"><span>${a}</span><p>${esc(b)}</p></div>`).join('')}</div>`}function renderBus(bus){$('#busOfficial').href=bus.source;$('#busUpdated').textContent=`Fonte: CENTRAN/UFPR • atualização ${bus.updated}. ${bus.note}`;const now=new Date(),dow=now.getDay(),cur=now.getHours()*60+now.getMinutes();let rows=dow===6?bus.saturday:bus.weekdays;if(dow===3)rows=[...rows,...bus.wednesdayExtra];if(dow===0)rows=[];const all=rows.flatMap(r=>r.times.map(t=>({line:r.line,time:t,min:+t.slice(0,2)*60 + +t.slice(3,5)}))).filter(x=>x.min>=cur).sort((a,b)=>a.min-b.min).slice(0,5);$('#nextBuses').innerHTML=all.length?all.map(x=>`<span class="next-bus"><strong>${esc(x.time)}</strong> · ${esc(x.line)}</span>`).join(''):'<span class="next-bus">Sem outros horários previstos hoje</span>';$('#busLines').innerHTML=rows.length?rows.map(r=>`<div class="bus-line"><strong>${esc(r.line)}</strong><div class="bus-times">${r.times.map(t=>`<span class="bus-time">${esc(t)}</span>`).join('')}</div></div>`).join(''):'<div class="campus-empty">Sem linhas regulares listadas para hoje. Consulte a fonte oficial.</div>'}load()})();